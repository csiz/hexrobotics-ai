{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "bun build src/index.html --outdir=dist",
    "dev": "bun --hot --open src/index.html"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
